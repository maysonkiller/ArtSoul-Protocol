# ArtSoul Durable Backlog

Updated: 2026-08-19
Canonical phase model: `A-D` from [`docs/canon/ARTSOUL_CANON_BIBLE_FULL.md`](canon/ARTSOUL_CANON_BIBLE_FULL.md) section 17.

This is the repository's durable work register. It consolidates verified completed work, open implementation work, founder-owned inputs, and code-level follow-ups so that ideas do not remain authoritative only in chat sessions or external prompt files.

## Rules

1. The Canon Bible is authoritative for architecture. This backlog schedules and records work; it does not amend canon.
2. Frozen economics may change only through a dated founder amendment. Primary remains `97.5 / 2.5`; resale remains `92.5 / 5.5 / 1 / 1`; all deposit, increment, duration, settlement, and default rules remain those in Bible section 3.
3. Genesis grant cadence, Ecosystem Pool contest triggers, legal entity form and jurisdiction, and launch-readiness windows are tunable or undecided placeholders. A roadmap estimate must never silently become canon, a contract constant, or a treasury rule.
4. Every new product or engineering idea from any chat, report, prompt, review, or partner conversation must first receive a sourced row here before implementation begins.
5. Status is evidence-based: `done`, `in progress`, `planned`, or `blocked-on-founder`. A chat claim alone is not completion evidence.
6. Work follows the canonical phases in order. Phase C contract or premium-status work does not displace unresolved Phase A acceptance blockers.
7. Resource-gated work follows [`RESOURCE_GATED_WORK.md`](RESOURCE_GATED_WORK.md). Missing funding defers activation; it never waives security, audit, role-separation, legal, migration, or mainnet acceptance requirements.

## Canonical Phase Mapping

| Canon phase | Purpose | Historical planning absorbed |
| --- | --- | --- |
| A - Stabilize Public Testnet | Close production-testnet blockers | Security, wallet, diagnostics, CI, indexer/projection verification, moderation MVP, Base commitments, current UI acceptance |
| B - Public Beta | Validate with real users | Cohort onboarding, incident operations, evidence-backed defect work, durable Snapshot A, build-in-public |
| C - Mainnet Preparation | Build and audit the final system | Contract rework, Genesis, Collections, Ecosystem Pool, admin/eligibility, auras/badges, founder infrastructure, final visual pass |
| D - Staged Mainnet Launch | Migrate and activate safely | Snapshot verification, destructive reset, Base deployment, Core stability window, separate Genesis pilot, controlled growth |

Earlier informal `A-G` plans are retired. Their stabilization/testnet work maps to A, beta validation to B, contract/security/legal/final-product work to C, and migration/deployment/activation to D. Historical token or multichain phases have no mapping and remain out of scope. Source: [`17_ROADMAP_PHASES.md`](canon/17_ROADMAP_PHASES.md) and [`CHANGELOG_2026-07-16_ROADMAP_ALIGNMENT.md`](canon/CHANGELOG_2026-07-16_ROADMAP_ALIGNMENT.md).

## Source Audit Boundary

This consolidation audited the repository through `main` commit `ab0b1d7`, [`PROJECT_STATE.md`](PROJECT_STATE.md), [`HANDOFF.md`](HANDOFF.md), the Canon Bible and split canon, [`CONTRACT_REWORK_PLAN.md`](canon/CONTRACT_REWORK_PLAN.md), merged and open pull-request descriptions from [PR #60](https://github.com/maysonkiller/ArtSoul-Protocol/pull/60) through [PR #110](https://github.com/maysonkiller/ArtSoul-Protocol/pull/110), and every current `TODO`/`FIXME` code search result.

`MAINNET_READINESS_AUDIT.md` was requested as a source but does not exist in the current tree or reachable repository history. No item below is attributed to that absent file. If it is recovered, its still-relevant evidence must be reconciled into this backlog in a separate docs-only update.

## Phase A - Stabilize Public Testnet

| ID | Item | Why/source | Status | Phase | Notes |
| --- | --- | --- | --- | --- | --- |
| A-01 | Complete secret-hygiene and deployment-security verification | Canon [A1](canon/12_IMPLEMENTATION_BACKLOG.md); [`PROJECT_STATE.md` P1](PROJECT_STATE.md); [PR #110](https://github.com/maysonkiller/ArtSoul-Protocol/pull/110); [`A1_CREDENTIAL_HISTORY_ACCEPTANCE_2026-08-07.md`](testnet/A1_CREDENTIAL_HISTORY_ACCEPTANCE_2026-08-07.md) | done | A | Accepted 2026-08-07. The tracked template contains empty server-secret placeholders, the local server credential decodes to `service_role` while remaining ignored and untracked, the historical secondary development key is absent locally and on Hetzner, and the public repository has Secret Scanning plus push protection enabled with zero open alerts. Repository history is intentionally retained under those controls; no secret value is recorded in Git. |
| A-02 | Verify RLS and reconcile the production migration ledger | Canon [A1](canon/12_IMPLEMENTATION_BACKLOG.md); [`PROJECT_STATE.md` P1/P2](PROJECT_STATE.md); [PR #110](https://github.com/maysonkiller/ArtSoul-Protocol/pull/110); [PR #116](https://github.com/maysonkiller/ArtSoul-Protocol/pull/116); [`A1_MOBILE_AUTH_UPLOAD_POLICY_ACCEPTANCE_2026-08-04.md`](testnet/A1_MOBILE_AUTH_UPLOAD_POLICY_ACCEPTANCE_2026-08-04.md); [`A1_CREDENTIAL_HISTORY_ACCEPTANCE_2026-08-07.md`](testnet/A1_CREDENTIAL_HISTORY_ACCEPTANCE_2026-08-07.md) | done | A | Accepted 2026-08-07. Phase 18.7b/18.7c were applied after a verified backup, the migration ledger was reconciled without invented rows, every public table has forced RLS, client roles hold no non-SELECT table grant, artwork writes have no client policy, and the canonical public artwork SELECT policy plus Storage MIME/50 MB guardrails remain active. Desktop and iOS SIWE, signed upload/public reads, and authenticated MIME/size rejection all passed. |
| A-03 | External-mobile wallet acceptance and Base Sepolia write guard | Canon [A2](canon/12_IMPLEMENTATION_BACKLOG.md); [PRs #60-#65](https://github.com/maysonkiller/ArtSoul-Protocol/pulls?q=is%3Apr+is%3Amerged+60..65), [#68](https://github.com/maysonkiller/ArtSoul-Protocol/pull/68), [#71-#85](https://github.com/maysonkiller/ArtSoul-Protocol/pulls?q=is%3Apr+is%3Amerged+71..85), [#88](https://github.com/maysonkiller/ArtSoul-Protocol/pull/88), [#92-#102](https://github.com/maysonkiller/ArtSoul-Protocol/pulls?q=is%3Apr+is%3Amerged+92..102), [#156](https://github.com/maysonkiller/ArtSoul-Protocol/pull/156); [`MOBILE_WALLET_PRODUCTION_ACCEPTANCE_2026-07-30.md`](testnet/MOBILE_WALLET_PRODUCTION_ACCEPTANCE_2026-07-30.md) | done | A | Production iOS Chrome + MetaMask acceptance confirmed live-session retention and automatic Ethereum Mainnet -> Base Sepolia recovery without the prior raw SDK errors. Base Sepolia remains the only operational write chain. This closes the wallet runtime defect but does not substitute for A1's fresh mobile SIWE and authenticated negative upload-policy evidence. |
| A-04 | Remove visible wallet diagnostics from production pages | Canon [A3](canon/12_IMPLEMENTATION_BACKLOG.md); [PR #104](https://github.com/maysonkiller/ArtSoul-Protocol/pull/104) | done | A | Repository search finds no production overlay wiring or `walletdebug` UI. The isolated `wallet-test.html` bench remains. |
| A-05 | Eliminate guest/restoring avatar and desktop header flicker | [PR #86](https://github.com/maysonkiller/ArtSoul-Protocol/pull/86), [PR #94](https://github.com/maysonkiller/ArtSoul-Protocol/pull/94), [PR #168](https://github.com/maysonkiller/ArtSoul-Protocol/pull/168), [PR #194](https://github.com/maysonkiller/ArtSoul-Protocol/pull/194); [`SHARED_HEADER_PRODUCTION_ACCEPTANCE_2026-08-04.md`](testnet/SHARED_HEADER_PRODUCTION_ACCEPTANCE_2026-08-04.md); [`SHARED_HEADER_PRODUCTION_REACCEPTANCE_2026-08-13.md`](testnet/SHARED_HEADER_PRODUCTION_REACCEPTANCE_2026-08-13.md); `header-prepaint.js`; `avatar-dropdown.js`; `unified-styles.css`; `test/guest-avatar.test.cjs`; `test/shared-header-navigation-flicker.test.cjs` | done | A | Re-accepted on production 2026-08-13 after PR #194. The founder exercised a real iOS device across connected, disconnected, explicit reconnect, SIWE, network recovery, reload and forward cross-document navigation. The guest/connected identity mismatch, duplicate control, blank account shell, header jump and ordinary-navigation flicker were not reproduced. The bounded prepaint bridge remains presentation-only: provider settlement is still authoritative for authentication and connected-only actions. A single whole-document repaint observed only during browser Back/history-swipe is separated as A-48 because it does not change account identity or reopen this header defect. |
| A-06 | Compact the header account button/dropdown and keep X/Discord in one mobile row | [PR #86](https://github.com/maysonkiller/ArtSoul-Protocol/pull/86) | done | A | Shared desktop/mobile menu geometry and the mobile profile social-row layout were implemented together. |
| A-07 | Preserve responsive shell/header consistency across product pages | [PR #62](https://github.com/maysonkiller/ArtSoul-Protocol/pull/62), [PR #64](https://github.com/maysonkiller/ArtSoul-Protocol/pull/64), [PR #69](https://github.com/maysonkiller/ArtSoul-Protocol/pull/69) | done | A | Completed responsive/header and mobile UI packs remain subject to normal regression testing. |
| A-08 | Revalidate every product HTML page after deploy | [PR #105](https://github.com/maysonkiller/ArtSoul-Protocol/pull/105) | done | A | Product HTML uses `max-age=0, must-revalidate`; immutable asset caching remains intact. |
| A-09 | Keep profile wallet address copy and BaseScan actions | [PR #103](https://github.com/maysonkiller/ArtSoul-Protocol/pull/103) | done | A | Current testnet links target Base Sepolia and are isolated from wallet-chain display state. |
| A-10 | Keep Base app domain verification metadata deployed | [PR #106](https://github.com/maysonkiller/ArtSoul-Protocol/pull/106) | done | A | Base dashboard ownership verification is part of the current public HTML. |
| A-11 | Preserve cached public artwork projections and narrow query shapes | [PR #70](https://github.com/maysonkiller/ArtSoul-Protocol/pull/70); `src/api/routes/public/artworks.js` | done | A | This is the required egress pattern for future public metrics: server projection cache plus CDN cache, never per-card fan-out. |
| A-12 | Preserve the deployed RPC diet | [PR #90](https://github.com/maysonkiller/ArtSoul-Protocol/pull/90); [`RPC_USAGE.md`](RPC_USAGE.md) | done | A | Public Base Sepolia RPC handles cheap reads first; configured RPCs remain fallbacks and indexer polling is bounded. |
| A-13 | Observe RPC/Supabase usage and configure cost alerts | Canon [A9](canon/12_IMPLEMENTATION_BACKLOG.md); [`PROJECT_STATE.md` P1](PROJECT_STATE.md); [PR #90](https://github.com/maysonkiller/ArtSoul-Protocol/pull/90); [`A9_INFRA_COST_MONITORING.md`](runbooks/A9_INFRA_COST_MONITORING.md) | done | A | Accepted 2026-07-28 after the consecutive 2026-07-22 through 2026-07-28 provider-dashboard window. Alchemy used 14.1K CUs during the window, forecast 22.9M of the 30M monthly hard limit, and retained 7.1M CUs of forecast headroom. ArtSoul-only Supabase uncached egress stayed at 41.0–59.4 MB/day; current-cycle uncached/cached totals were 0.246/0.764 GB and Spend Cap remained enabled. One 676.6 MB partial-day cached spike was bounded well inside the 250 GB allowance and was not a repeated trend. Native custom Alchemy alerts remain unavailable on the current plan and Supabase has no fine-grained budget-threshold notifications, so the hard controls, tested health checker, and Tuesday/Friday manual review remain the accepted no-cost path. |
| A-14 | Reconcile indexer confirmation-depth reporting and restart behavior | Canon [A5](canon/12_IMPLEMENTATION_BACKLOG.md); [`PROJECT_STATE.md` P1](PROJECT_STATE.md); [PR #112](https://github.com/maysonkiller/ArtSoul-Protocol/pull/112); [PR #114](https://github.com/maysonkiller/ArtSoul-Protocol/pull/114) | done | A | PR #112 reconciled the deployed Base Sepolia depth from 12 to 3 without resetting the persisted cursor. PR #114 added schema-aware migration 014, regression coverage and a PostgreSQL 17 integration check for deployments without the superseded legacy projection tables. Migration 014 was applied after backup and its guards were verified; Hetzner then restarted on merge commit `4a10841`. Health remained healthy at depth 3 with an advancing cursor, no sync error, zero unresolved errors and no new missing-legacy-table or reorg-check failure in the observed post-restart cycles. |
| A-15 | Resolve the optional `failed_events` metric/table and close the event-failure integrity gap | Canon [A9](canon/12_IMPLEMENTATION_BACKLOG.md); [`PROJECT_STATE.md` P1](PROJECT_STATE.md); [PR #136](https://github.com/maysonkiller/ArtSoul-Protocol/pull/136); [`A9_INFRA_COST_MONITORING.md`](runbooks/A9_INFRA_COST_MONITORING.md) | done | A | PR #136 replaced the never-created `failed_events` subsystem with fail-closed `event_processing_registry` handling: failures are UPSERTed outside the rolled-back transaction, an incomplete range advances no cursor and is retried, confirmation is clamped to indexed blocks, and `/health` plus Prometheus report failed/dead counts. Production was deployed on 2026-07-22 at merge commit `32b2d49`; PM2 remained online, `/health` was `healthy` at depth 3 with four blocks of lag, zero unresolved errors, and `eventFailures={failed:0,dead:0}`. Authenticated Prometheus output independently reported both failure gauges at zero. No `failed_events` table or migration is required. |
| A-16 | Establish green cross-platform CI and deterministic Hardhat execution | Canon [A4](canon/12_IMPLEMENTATION_BACKLOG.md); [PR #109](https://github.com/maysonkiller/ArtSoul-Protocol/pull/109) | done | A | PR #109 is merged. Reproducible `npm ci`, build, static checks, the canonical Node suite, and 19 contract tests run on Ubuntu and Windows CI. |
| A-17 | Include currently uncollected `.mjs`/`.js` tests in the canonical runner | [PR #109 known gap](https://github.com/maysonkiller/ArtSoul-Protocol/pull/109); [PR #132](https://github.com/maysonkiller/ArtSoul-Protocol/pull/132); `scripts/run-unit-tests.mjs` | done | A | PR #132 extends the canonical runner to collect `.test.cjs`/`.test.mjs`/`.test.js` in deterministic alphabetical order while keeping `ArtSoulV41.test.cjs` in the separate Hardhat suite. It restores `test/artwork-visibility-api.test.mjs` and `test/resale-eligibility.test.js`, adds collection regression coverage, and passes the full Windows and Ubuntu CI matrix. |
| A-18 | Verify projections and complete provenance on every public surface | Canon [A6](canon/12_IMPLEMENTATION_BACKLOG.md); [PR #120](https://github.com/maysonkiller/ArtSoul-Protocol/pull/120); [PR #122](https://github.com/maysonkiller/ArtSoul-Protocol/pull/122); [PR #124](https://github.com/maysonkiller/ArtSoul-Protocol/pull/124) | done | A | Creator, First Collector, Owner, auction, settlement, resale, moderation and pending-projection states are verified on every public surface. Compact preview cards stay creator-focused (public nickname or shortened address); full First Collector/Owner provenance and the timeline are verified on the detail surface; Owned includes creator buybacks. Hidden-artwork unavailable copy stays with A8. |
| A-19 | Verify profile lifecycle and action gating | Canon [A7](canon/12_IMPLEMENTATION_BACKLOG.md); [`PROJECT_STATE.md` P1](PROJECT_STATE.md); [PR #126](https://github.com/maysonkiller/ArtSoul-Protocol/pull/126); [PR #127](https://github.com/maysonkiller/ArtSoul-Protocol/pull/127) | done | A | Production acceptance passed on 2026-08-09. Founder wallet `0x6EC8…989B` published artwork `v41:84532:27`; the exact-artwork redirect was correct, registration transaction `0x77b4895b8db42d4aeb272290f37e809460ed4e162e5eacad27172e43564ee5b6` created artwork `27`, and the one distinct `createAuction` transaction `0x5ed747634b4e88898eacf399b7c65d3cd498b5f8209b92642c0e91c25053759d` created auction `27`. Indexed provenance is complete and contains one registration followed by one auction start. |
| A-20 | Preserve staff hide/unhide and persistent moderation audit data | [PR #11](https://github.com/maysonkiller/ArtSoul-Protocol/pull/11), [PR #12](https://github.com/maysonkiller/ArtSoul-Protocol/pull/12); `src/api/routes/moderation/artwork-visibility.js` | done | A | Existing functionality is staff visibility control, not the complete complaint-driven moderation MVP. Production migration verification remains under A-02. |
| A-21 | Add the user Report button and copyright complaint form | Canon [A8](canon/12_IMPLEMENTATION_BACKLOG.md); [`07_ADMIN_MODERATION_CANON.md`](canon/07_ADMIN_MODERATION_CANON.md); [`PROJECT_STATE.md`](PROJECT_STATE.md); [PR #130](https://github.com/maysonkiller/ArtSoul-Protocol/pull/130); [`A8_MODERATION_ROLLOUT.md`](runbooks/A8_MODERATION_ROLLOUT.md) | in progress | A | PR #130 merged the intake behind a disabled feature flag with a private complaint schema, atomic wallet-authenticated submission, concurrent deduplication, and append-only evidence. The approved controlled-beta limit is five new reports per reporter wallet across a rolling 24-hour window, supplied explicitly through the environment. Different reporter wallets remain independent records; one wallet's duplicate pending category for the same artwork resolves to the existing record. Keep intake disabled until A-22 provides an operational review path and the migration/activation runbook passes. Submission never auto-hides an artwork. |
| A-22 | Add review queue, notifications and moderation audit-log UI | Canon [A8](canon/12_IMPLEMENTATION_BACKLOG.md); [`PROJECT_STATE.md`](PROJECT_STATE.md); independent review of [PR #130](https://github.com/maysonkiller/ArtSoul-Protocol/pull/130); [PR #133](https://github.com/maysonkiller/ArtSoul-Protocol/pull/133); [`A8_MODERATION_ROLLOUT.md`](runbooks/A8_MODERATION_ROLLOUT.md); [`A8C_PROTOCOL_ADMIN_REVIEW.md`](runbooks/A8C_PROTOCOL_ADMIN_REVIEW.md) | in progress | A | PR #133 merged the dedicated feature-flagged Protocol Admin page, lazy server-confirmed menu discovery, SIWE + active role + 15-minute passkey re-check on every protected API, independent-report presentation, serialized transitions, append-only decision reasons, durable notification obligations, untrusted-text rendering and keyboard focus containment. Its reviewed state machine includes the distinct `resolved` status, duplicate-pending reopen guard, transition-accurate restore events and multi-report hide protection. Implementation review is complete; the item remains in progress only because the ordered A8 migrations, Safe-only recovery, founder passkeys, production admin acceptance and public-intake activation are not complete. Critical/irreversible actions remain multisig-gated. |
| A-23 | Publish controlled-beta entry checklist and go/no-go evidence | Canon [A10](canon/12_IMPLEMENTATION_BACKLOG.md); [`HANDOFF.md`](HANDOFF.md); [`CONTROLLED_BETA_ENTRY.md`](testnet/CONTROLLED_BETA_ENTRY.md); [PR #157](https://github.com/maysonkiller/ArtSoul-Protocol/pull/157) | in progress | A | PR #157 prepares the entry pack, tester checklist, support path, issue template, prototype deviations, pause criteria, and evidence-based go/no-go record. Status remains NO-GO until every Phase A gate is evidenced and no P1 issue is open. |
| A-24 | Put “collector demand comes before minting” on the first screen | Canon [A11](canon/12_IMPLEMENTATION_BACKLOG.md); [`PROJECT_STATE.md` section 13](PROJECT_STATE.md); [PR #160](https://github.com/maysonkiller/ArtSoul-Protocol/pull/160) | done | A | Accepted on production 2026-07-28. The homepage hero leads with the demand-before-minting value proposition at desktop and mobile widths. |
| A-25 | Add the compact three-step product explanation | Canon [A11](canon/12_IMPLEMENTATION_BACKLOG.md); [`17_ROADMAP_PHASES.md`](canon/17_ROADMAP_PHASES.md); [PR #160](https://github.com/maysonkiller/ArtSoul-Protocol/pull/160) | done | A | Accepted on production 2026-07-28. The path is Publish -> build collector demand through auction/discovery signals -> settle and mint; desktop renders three columns and mobile renders one column without horizontal overflow. |
| A-26 | Add public artists/auctions/collectors/settled-volume metrics | Canon [A11](canon/12_IMPLEMENTATION_BACKLOG.md); [PR #70](https://github.com/maysonkiller/ArtSoul-Protocol/pull/70) cache precedent; [PR #160](https://github.com/maysonkiller/ArtSoul-Protocol/pull/160); [`A11_PUBLIC_METRICS_ROLLOUT.md`](runbooks/A11_PUBLIC_METRICS_ROLLOUT.md) | done | A | Accepted on production 2026-07-28. Migration 015 is installed with an idempotent event ledger and one chain-scoped aggregate that reverses automatically with contract-event reorg rollback. Database cross-checks matched all four values; the public list API reads only the Base Sepolia aggregate row inside the existing server/CDN cache, and the homepage reuses that response without an additional browser request or RPC. |
| A-27 | Align homepage positioning with ArtSoul as a curation layer | Canon [A11](canon/12_IMPLEMENTATION_BACKLOG.md); [`17_ROADMAP_PHASES.md`](canon/17_ROADMAP_PHASES.md); [PR #160](https://github.com/maysonkiller/ArtSoul-Protocol/pull/160) | done | A | Accepted on production 2026-07-28. The homepage states that collector conviction and discovery signals, not minting volume, determine what surfaces. |
| A-28 | Remove stale active-network and trust copy | Canon [A12](canon/12_IMPLEMENTATION_BACKLOG.md); [PR #162](https://github.com/maysonkiller/ArtSoul-Protocol/pull/162); [PR #164](https://github.com/maysonkiller/ArtSoul-Protocol/pull/164); [`A12_NETWORK_COPY_ACCEPTANCE.md`](testnet/A12_NETWORK_COPY_ACCEPTANCE.md); `README.md`; `index.html`; `visual-lab.html`; `src/entries/docs-protocol.jsx`; `avatar-dropdown.js`; `docs/testnet/WALLET_QA_CHECKLIST.md` | done | A | Accepted on production 2026-07-28. Active public copy identifies Base Sepolia as the only product testnet and Base as the production chain, the account menu offers only Base Sepolia as a product-network switch, and Protocol Docs use the canonical Genesis trust weight of `1.3x`. Historical Ethereum Sepolia data remains readable and write-disabled. |
| A-29 | Add a client-side live-auction countdown pill to shared cards | `src/ui/components/artwork-card.js` (`statusInfo`); detail countdown in `src/entries/artwork.jsx`; [PR #125](https://github.com/maysonkiller/ArtSoul-Protocol/pull/125) | done | A | PR #125 merged on 2026-07-20. Shared cards reuse the projected auction end time with one local timer across card surfaces and add zero API or RPC requests. |
| A-30 | Preserve media preview revival after browser Back navigation | `src/ui/components/artwork-card.js` (`reviveMediaPreviews`, `pageshow`); [PR #47](https://github.com/maysonkiller/ArtSoul-Protocol/pull/47) | done | A | Shared card media is re-prepared after bfcache/back-forward restore across surfaces. |
| A-31 | Preserve the site-wide ArtSoul audio-disc preview | `src/ui/components/artwork-card.js`; `unified-styles.css`; [PR #47](https://github.com/maysonkiller/ArtSoul-Protocol/pull/47) | done | A | Desktop idle rotates slowly, active playback speeds up, and mobile idle is static. |
| A-32 | Preserve artwork media fit/fullscreen and mobile ownership-row fixes | [PR #54](https://github.com/maysonkiller/ArtSoul-Protocol/pull/54); `src/entries/artwork.jsx` | done | A | Detail media uses stable cover framing and original-aspect fullscreen behavior; shared card previews were not changed by that fix. |
| A-33 | Complete an evidence-backed artwork-page acceptance/polish sweep | [PR #69](https://github.com/maysonkiller/ArtSoul-Protocol/pull/69); canon [A6-A7](canon/12_IMPLEMENTATION_BACKLOG.md) | planned | A | Verify current mobile/desktop composition, transaction handoffs and action visibility. Open only concrete defects; do not redesign or change auction mechanics. |
| A-34 | Build one reusable, presentation-only aura frame shell | [`16_VISUAL_STATUS_AND_THEME_CANON.md`](canon/16_VISUAL_STATUS_AND_THEME_CANON.md); `unified-styles.css` artwork-card TODO | planned | A | The current tree has no aura component. This item creates one theme-safe frame API only; it must not guess eligibility. Real on-chain/status binding remains C-14. |
| A-35 | Consolidate/document the migration trees and legacy runtime boundaries | [`PROJECT_STATE.md` P2](PROJECT_STATE.md); [PR #110](https://github.com/maysonkiller/ArtSoul-Protocol/pull/110); [`MIGRATION_RUNBOOK.md`](security/MIGRATION_RUNBOOK.md); `test/migration-ledger-coverage.test.cjs` | in progress | A | **First half done: the ledger.** An audit of the three trees found 33 SQL files of which **12 were named in no ledger at all**, including all four A8 migrations that are the first step of the only chain gating Phase B. The runbook now carries a per-file disposition for every file in every tree, and a coverage test fails when a new migration is added without one, so the ledger cannot silently go stale again - which is how it got this way. Files with no evidence are marked `not recorded`, which explicitly means the repository holds no proof of their production status and is **not** a claim that they were never applied; inventing a status would make the ledger worse than none. The runbook also gained an A8 activation section, because the order lived only in `runbooks/A8_MODERATION_ROLLOUT.md` while backup and verification discipline lived only in the runbook, so an operator following either document alone was missing half the procedure. The `001` prefix collision between `migrations/001_ai_integration.sql` and the indexer `001` is now documented as real. **Second half still open:** legacy Express, moderation and global-script boundaries are untouched. Nothing was deleted, per this row's own instruction to reconcile before removing. |
| A-36 | Keep OpenAI Build Week engineering evidence current | [PR #87](https://github.com/maysonkiller/ArtSoul-Protocol/pull/87); `README.md` | done | A | Public README records the Codex workflow without claiming AI controls protocol outcomes. |
| A-37 | Preserve the owner-only NFT resale listing action | [PR #63](https://github.com/maysonkiller/ArtSoul-Protocol/pull/63); `src/features/marketplace/resale-eligibility.js`; `src/entries/artwork.jsx` | done | A | Existing testnet listing UI reuses the deployed resale method and canonical-floor guard. The deployed split deviation is not accepted for mainnet and remains C-07/C-08 work. |
| A-38 | Triage dependency and production frontend warnings without forced upgrades | Reproducible `npm ci` audits on 2026-07-17 and 2026-07-28; A11 production browser acceptance; GitHub Actions run `30385084599` | planned | A | The 2026-07-28 Hetzner install reported 19 low, 12 moderate, and 24 high findings, superseding the older count. Classify runtime versus development-only paths, then update focused packages with full CI. The same acceptance observed the production Tailwind CDN warning and unused Reown font-preload warning. CI also warns that `actions/checkout@v4` and `actions/setup-node@v4` target deprecated Node 20 and are being forced onto Node 24. Remove, justify, or upgrade each item during the focused dependency/performance pass. Do not run `npm audit fix --force` or accept breaking upgrades blindly. |
| A-39 | Build A8a moderation step-up behind a disabled feature flag | Canon [A8](canon/12_IMPLEMENTATION_BACKLOG.md); [`RESOURCE_GATED_WORK.md`](RESOURCE_GATED_WORK.md); [PR #129](https://github.com/maysonkiller/ArtSoul-Protocol/pull/129); [`A8D_SAFE_RECOVERY.md`](runbooks/A8D_SAFE_RECOVERY.md) | in progress | A | A8a is merged with the flag disabled and migration unapplied. A8d now supplies the inert Safe-only recovery implementation, additive migration and verification, but nothing is configured or activated and the founder ceremony is not rehearsed. Activation still requires ordered migrations, archived verification, two founder passkeys, the one-time audited bootstrap grant, all Safe recovery success/denial rehearsals, and a reviewed deployment. The 2026-08-16 multisig rehearsal ([`RG05_SAFE_MULTISIG_REHEARSAL_2026-08-16.md`](testnet/RG05_SAFE_MULTISIG_REHEARSAL_2026-08-16.md)) does **not** advance this item: it exercised value transfers and `swapOwner`, while A8d requires EIP-1271 verification of a server-issued recovery message through two configured RPC endpoints plus the eleven mandatory denial cases in [`A8D_SAFE_RECOVERY.md`](runbooks/A8D_SAFE_RECOVERY.md) section 6. None of those has been rehearsed. X/Discord handles are eligibility/profile data, not authentication factors. |
| A-40 | Remove or rehabilitate the dormant in-process indexer alert path | A-13 audit; `src/indexer/production-runner.js` (`ALERT_WEBHOOK`, `_checkAlerts`); [PR #138](https://github.com/maysonkiller/ArtSoul-Protocol/pull/138) | done | A | PR #138 removed the never-invoked webhook path and mismatched thresholds. `/health` derives its RPC error and latency fields from the event listener's maintained rolling health windows; false block/event throughput zeroes are removed. Production acceptance passed on 2026-07-22 UTC at merge commit `711027b`: the monitor returned `ok=true`, PM2 kept the Base Sepolia indexer online, and `/health` reported numeric RPC latency with zero rolling RPC errors and no removed throughput placeholders. |
| A-41 | Make the indexer event heartbeat cancellable instead of a blocking 30-second sleep | A-15 audit ([PR #136](https://github.com/maysonkiller/ArtSoul-Protocol/pull/136)); `src/indexer/sync-engine.js` heartbeat loop; [PR #140](https://github.com/maysonkiller/ArtSoul-Protocol/pull/140) | done | A | PR #140 replaced the blocking sleep with a wakeable, resolve-only timer while preserving the 30000 ms production cadence and exactly-once client release. Production acceptance passed on 2026-07-22 UTC at merge commit `c1decb2`: the focused seven-test suite passed in 0.63 seconds on Hetzner without a global timer shim, PM2 kept Base Sepolia online, the monitor returned `ok=true`, and `/health` was healthy with eight blocks of lag, zero unresolved/RPC errors, and zero failed/dead events. The separate transactional visibility mismatch was subsequently resolved and accepted as A-43. |
| A-42 | Require explicit production credentials for the Prometheus endpoint | A-15 post-deploy verification; `src/indexer/production-runner.js` (`METRICS_AUTH`); [`A9_INFRA_COST_MONITORING.md`](runbooks/A9_INFRA_COST_MONITORING.md); [PR #142](https://github.com/maysonkiller/ArtSoul-Protocol/pull/142) | done | A | PR #142 removed the public-code `admin:changeme` fallback, requires a non-empty `METRICS_AUTH` before construction/listen/start, compares credentials with SHA-256 plus `crypto.timingSafeEqual`, and binds the HTTP endpoint to `127.0.0.1` only. Production acceptance passed on 2026-07-24 at merge commit `1c37061`: the focused suite passed 15/15 on Hetzner, PM2 kept the Base Sepolia indexer online, unauthenticated `/metrics` returned 401, authenticated `/metrics` returned 200 without exposing the credential or response body, the only port 3001 listener was `127.0.0.1:3001`, `monitor:indexer` returned `ok=true` at confirmation depth 3 with seven blocks of lag and zero unresolved/RPC errors, and the accepted PM2 state was saved. |
| A-43 | Resolve the event heartbeat/reaper transactional visibility mismatch | A-41 diagnosis ([PR #140](https://github.com/maysonkiller/ArtSoul-Protocol/pull/140)); [`A9_INFRA_COST_MONITORING.md`](runbooks/A9_INFRA_COST_MONITORING.md); [PR #144](https://github.com/maysonkiller/ArtSoul-Protocol/pull/144); [PR #145](https://github.com/maysonkiller/ArtSoul-Protocol/pull/145) | done | A | PR #144 uses a short committed claim before the projection transaction, a visible pooled heartbeat, and owner-plus-attempt fencing on heartbeat, completion and failure writes. Only `completed` is an idempotent success; an unavailable live lease fails the range closed and pins the cursor after a crash. The reaper uses `FOR UPDATE SKIP LOCKED`, marks abandoned attempts as visible retryable failures, preserves the previous owner for evidence, and leaves `retry_count` unchanged until the next actual acquisition. Existing schema columns are sufficient, so no production migration was required. Production acceptance passed on 2026-07-26 UTC after the reproducible-install repair in PR #145: the focused suite passed 19/19 on Hetzner, the cursor advanced from 44641103 to 44641118, `monitor:indexer` returned `ok=true`, `/health` was healthy at confirmation depth 3 with four blocks of lag and zero unresolved/RPC/failed/dead errors, the registry contained 90 completed Base Sepolia events and no other status, the stale-processing query returned zero rows after the lease/reaper window, and PM2 saved Base Sepolia online while retired Ethereum Sepolia remained stopped. |
| A-44 | Prevent async module-entry DOM mount races | A12 production acceptance; [PR #163](https://github.com/maysonkiller/ArtSoul-Protocol/pull/163); [PR #164](https://github.com/maysonkiller/ArtSoul-Protocol/pull/164); [`A12_NETWORK_COPY_ACCEPTANCE.md`](testnet/A12_NETWORK_COPY_ACCEPTANCE.md); `docs-protocol.html`; `scripts/verify-build.mjs` | done | A | Accepted on production 2026-07-28. Cached Protocol Docs had executed its async React entry before `#app` existed, causing React error 299 and an empty page. PR #163 removed `async` from every HTML module entry, source/build checks prevent recurrence, and Protocol Docs plus representative Gallery/Profile routes rendered without console errors or horizontal overflow after deployment. |
| A-45 | Keep the shared header avatar and identity bound to the connected wallet | Production iOS Chrome report after A-03/[PR #156](https://github.com/maysonkiller/ArtSoul-Protocol/pull/156) and [PR #166](https://github.com/maysonkiller/ArtSoul-Protocol/pull/166); `avatar-dropdown.js`; `src/entries/profile.jsx`; [PR #167](https://github.com/maysonkiller/ArtSoul-Protocol/pull/167); [PR #168](https://github.com/maysonkiller/ArtSoul-Protocol/pull/168); [`SHARED_HEADER_PRODUCTION_ACCEPTANCE_2026-08-04.md`](testnet/SHARED_HEADER_PRODUCTION_ACCEPTANCE_2026-08-04.md) | done | A | Accepted 2026-08-04 after production desktop and real-mobile testing. PR #167 bound profile resolution to the active wallet, rejected superseded reads, retried failed identity reads only through bounded events, and cleared wallet-bound identity on disconnect. PR #168 removed the remaining split visual commit and first-paint flicker. The accepted run kept avatar, profile name and shortened address consistent across navigation and reload without changing wallet lifecycle, SIWE, contracts, economics or the Base-only write guard. |
| A-46 | Unblock first paint without regressing the shared header | Measured 2026-08-10; [PR #184](https://github.com/maysonkiller/ArtSoul-Protocol/pull/184); [PR #194](https://github.com/maysonkiller/ArtSoul-Protocol/pull/194); [`SHARED_HEADER_PRODUCTION_REACCEPTANCE_2026-08-13.md`](testnet/SHARED_HEADER_PRODUCTION_REACCEPTANCE_2026-08-13.md) | done | A | Re-accepted on production 2026-08-13. The heavy account component remains deferred, while the hard-size-limited `header-prepaint.js` bridge provides one coherent first-frame presentation without becoming an authentication source. The artwork detail document exposes its static skeleton before React and a bounded module-load failure state, so a delayed or failed module no longer leaves a black page. Production desktop automation plus real-iOS connected/disconnected navigation confirmed the first-paint repair without regressing wallet identity or ordinary page navigation. |
| A-47 | Reduce head asset weight now that first paint is unblocked | Re-measured 2026-08-11 | planned | A | The original entry measured the list-shaped `/api/public/artworks` route at ~1.4s. Its steady state is 0.13-0.30s for 63 kB, and it remained 0.28s after 50s idle, so that route is not the bottleneck. The exact-artwork route used by `/artwork/:id` has a different cold path: production acceptance after PR #189 measured 2.27-3.26s for the first request to a fresh function instance and 0.24-0.39s warm. PR #189 removed a separate client-side wait of up to 5s by consuming the existing `artsoul:db-ready` signal, but it did not remove this server cold start. The A-46 regression repair makes that wait visible and failure-safe with a static skeleton, but deliberately does not claim to make the endpoint faster. Do not hide the remaining latency with unconditional CDN caching: exact projections include live auction state, and stale bids are not an acceptable speed trade. Measure and optimize the direct projection path separately, or prove a cache split between immutable artwork facts and live auction data before changing response caching. After A-46 no head script blocks first paint, but the page still pulls ~768 kB of same-origin JS/CSS, which now delays interactivity rather than paint: appkit-init.js 168 kB, vendor-react 140 kB, the merged unified-styles/mobile-no-motion chunk 108 kB, avatar-dropdown.js 100 kB. Checked and rejected: the CSS is not double-downloaded - Vite folds unified-styles.css into the mobile-no-motion chunk and the page never requests the raw copy. Real options are splitting avatar-dropdown.js and reducing the exact-artwork cold path without weakening live-auction freshness. Measure before assuming again. **Investigated 2026-08-11 - lazy-loading the wallet SDK is NOT a small change.** `appkit-init.js` is already `type="module"`, so it is deferred and does not block first paint; the only gain would be 168 kB of bandwidth for visitors who never touch a wallet. But it is the sole writer of `window.artsoulWalletStateSettled` and the sole dispatcher of `artsoul:wallet-state-changed`, and the gate only opens on a connected state or on an explicitly finalized guest state after reconciliation. Deferring it leaves the header in `Connecting...` forever and freezes the artwork action gating, so that experiment remains rejected. |
| A-48 | Diagnose the single full-document repaint on browser Back/history-swipe | Founder production report 2026-08-13; `src/entries/artwork.jsx` (`window.history.back()`); `src/ui/navigation-manager.js`; `src/ui/components/artwork-card.js`; WebKit and Chrome back-forward probes; [PR #203](https://github.com/maysonkiller/ArtSoul-Protocol/pull/203) | in progress | A | Ordinary forward navigation is accepted and stable. WebKit first reproduced a `back_forward` navigation with `pageshow.persisted=false` and a fresh document load, with no guest/connected identity transition. A production Chrome DevTools probe then identified the application-owned cause: `Page.backForwardCacheNotUsed` reported `NetworkExceedsBufferLimit` when the gallery was left immediately after its result count appeared. Nearly every full-size Supabase card image was still in flight because the shared card renderers did not defer offscreen images. A ten-second control run, after those transfers completed, restored the same gallery from bfcache with `pageshow.persisted=true`. PR #203 adds native lazy loading and asynchronous decoding to every shared DOM/React card image path and the gallery fallback; it does not change history semantics, AppKit, relay handling, wallet state or protocol data. Regression coverage pins the shared image policy. Status stays `in progress` until production desktop and iOS/Android Back acceptance confirms that cards still appear correctly and the single repaint is gone or materially bounded during a fast open-and-return flow. |
| A-49 | Restore X account linking on the apex origin | Founder production report 2026-08-19; [`SOCIAL_LINKING_SETUP.md`](SOCIAL_LINKING_SETUP.md); `src/api/routes/oauth.js`; [`RG01_APEX_ORIGIN_SMOKE_CHECKLIST.md`](testnet/RG01_APEX_ORIGIN_SMOKE_CHECKLIST.md) | done | A | Resolved and verified on production 2026-08-19: the founder linked and unlinked X on the apex successfully after the portal correction. X returned `Something went wrong / You weren't able to give access to the App` on its own authorize screen at `https://artsoulprotocol.com`. Discord linking on the same origin works. Server code is not implicated: `requestOrigin` accepts the apex, `callbackUri` builds `https://artsoulprotocol.com/api/oauth/callback/twitter`, that value is persisted in the OAuth state and reused unchanged for the token exchange, and PKCE `S256` is generated correctly. The failing request also carries the same OAuth 2.0 client ID that `SOCIAL_LINKING_SETUP.md` documents, so the Vercel configuration points at the intended X application. The remaining explanation is the X application's own **User authentication settings**: the apex callback URL is absent from its Callback URI list, or those settings were reset by an app-type or credential change after the 2026-08-08 domain cutover. The apex social-link path was never verified before, which is exactly what RG-01 exists to catch. **Root cause confirmed and corrected in the portal on 2026-08-19.** The application's Callback URI list held only the old `artsoul.vercel.app` origin, `localhost:3000`, and a dead `fix/social-linking` preview alias; the apex was absent entirely. The apex callback was added, the dead preview entry was removed, and Website/Organization/Terms/Privacy URLs were moved from the Vercel origin to the apex. App permissions, app type and keys were not changed, so no Vercel variable and no redeploy are required. The application was also renamed to `ArtSoul Protocol`, its description was corrected from the inaccurate `Decentralized Art Marketplace`, and `Request email from users` was disabled because the flow requests only `tweet.read users.read` and never uses an email address. Do not weaken origin validation if it recurs. |
| A-50 | Restore the Edit Profile action when a profile is opened from an artwork | Founder production report 2026-08-19; `src/entries/profile.jsx` (`refreshProfileState`, `loadProfile`); `src/features/profile/profile-ownership.js`; `test/profile-ownership-late-wallet.test.js` | in progress | A | Opening your own profile from the header shows **Edit Profile**; reaching the same profile from an artwork, which navigates to `/profile?address=0x...`, hides it. Both paths render the same wallet's profile, so this is an ownership-detection defect, not a permissions change. The root cause is in `refreshProfileState`: `const stateIsSettled = Boolean(viewAddress) || window.artsoulWalletStateSettled === true` treats *an address being present in the URL* as proof that **wallet state has settled**. Those are unrelated facts. With `?address=` in the URL the guard opens immediately, before the wallet provider has produced an address, so `getActiveWalletAddress()` returns empty. The ownership write is then skipped entirely, because it is guarded by `if (viewedAddress && confirmedAddress)`, and `loadProfile` separately computes `isOwn` from the same empty address and commits `setIsOwnProfile(false)`. Recovery depends on a later wallet event arriving to re-run the check; when the state was already settled before mount, that event may never fire and the button stays hidden. Fix the guard so URL presence never substitutes for wallet settlement, and make ownership resolve from the settled wallet address rather than from whichever value happens to exist at first paint. Do not gate the button on the absence of the `address` parameter: that would hide it for a legitimate self-view and is the same class of mistake. Fixed by extracting the decision into the pure `resolveProfileOwnership` and `isWalletStateSettled` helpers, which both call sites now share. Target resolution and wallet settlement are separated: a URL address still selects which profile to render but no longer opens the settlement guard. Ownership returns `null` while unsettled, and `null` is never committed, so an unsettled read cannot survive as a false. `isOwnProfile` now initialises to `false` so an unverified owner-only action is never rendered. Five regression tests cover late settlement, checksum-case mismatch, another wallet's profile, a settled guest, and the source shape that would reintroduce the conflation. `npm test` green: 639 unit and 19 contract tests. Status stays `in progress` until production acceptance confirms the button returns when a profile is opened from an artwork. Preserve A-05/A-45 header behaviour. |
| A-51 | Remove expected-state console errors from a fresh profile load | Founder desktop console capture on a new account, production, 2026-08-19; `supabase-client.js`; `contracts-integration.js`; `src/entries/profile.jsx` | done | A | A first visit produced two console errors for states that are normal. `GET /rest/v1/profiles?...` returned **HTTP 406** because the profile read used `single()`, and PostgREST answers zero rows that way; the caller already treated it as normal, but the browser reported a failed request. The read now uses `maybeSingle()`, so the dead `PGRST116` special case is gone with it. Separately, `Could not load Genesis state: Error: Contracts not initialized` appeared whenever the wallet provider had not arrived by the time the profile mounted; Genesis state is presentational, so `isReady()` was added to the contract layer and the profile returns its fallback instead of reporting an error. Only the profile read changed: lookups addressing one row by id still use `single()` and must keep failing loudly. Status stays `in progress` until a fresh-account production load is confirmed clean. Closed 2026-08-21 by browser verification on the apex and on the PR preview at 375px: the only console output is third-party AppKit telemetry - a `fonts.reown.com` preload warning and one 403 from `pulse.walletconnect.org/batch`. No ArtSoul error is emitted on a fresh profile or gallery load. Those two belong to the Reown SDK and are not ours to silence. **Verified by measurement in a browser, not by founder acceptance on a device.** |
| A-52 | Decide and unify the identity label shown before a nickname exists | Founder desktop report 2026-08-19 and iOS/Android production captures 2026-08-20; `avatar-dropdown.js`; `header-prepaint.js`; `supabase-client.js` `displayName()`; `unified-styles.css`; `test/identity-label-single-rule.test.cjs`; `test/guest-avatar.test.cjs` | done | A | **Resolved by removing the placeholder rather than choosing a word.** The project already had a canonical rule in `supabase-client.js` `displayName()`: a real username wins, otherwise the account is named by its shortened wallet address. `ArtSoul User` was never a design choice - it only appeared while that module had not loaded, so the header showed a placeholder and then replaced it with the address, which is what read as the label changing its mind. Every `avatar-dropdown.js` fallback now derives the name from the address it already holds, and one of them was outright wrong: the generic painter fell back to `ArtSoul Guest` even with an address in hand, painting a connected wallet as a guest. `ArtSoul Guest` survives only where it is true, meaning no connected wallet at all, which is the prepaint disconnected boot. Follow-up mobile evidence showed that the correct name and address were already present in the stable DOM but intentionally hidden by the final `max-width: 768px` layout, leaving only the avatar visible. The shared mobile header now allocates a fixed 168px account control and exposes the same atomic name/address column as desktop; no wallet state, provider, authorization or profile data rule changes. The prepaint resolving label is unchanged and the bridge stays presentation-only, so A-46 is untouched. Status stays `in progress` until production iOS and Android acceptance confirms that the name/address remain visible and the header does not shift across guest, resolving and connected states. Closed 2026-08-21 by browser verification: a disconnected header reads `ArtSoul Guest`, `ArtSoul User` appears nowhere in the product, and the canonical rule stays in `supabase-client.js` `displayName()`. **Verified by measurement in a browser, not by founder acceptance on a device.** |
| A-53 | Close the identity settle gap between the shared header and the profile page | Founder desktop report 2026-08-19 with console capture; founder Android production captures 2026-08-20; `avatar-dropdown.js`; `src/entries/profile.jsx`; `test/shared-header-avatar-consistency.test.cjs`; `test/profile-ownership-late-wallet.test.js` | in progress | A | With the profile page already open, connecting a wallet paints the avatar and nickname in the header noticeably before the profile body shows them, and the header sits at `Connecting...` for seconds on load and reload. Android captures added a second symptom: opening the menu during restoration showed `Connect Wallet` even though the previous session was actively restoring and later returned. The root cause is a presentation split, not a lost session. `renderResolvingState` deliberately rendered the guest account action while `appkit-init.js` correctly withheld a settled answer, and the profile refused to start even a public profile read until that answer arrived. The resolving menu now exposes neither Connect nor Disconnect; it reports a non-interactive `Restoring wallet…` status until the provider confirms one outcome. The profile may prefetch public data for the stored address during that interval, but ownership stays `null`/false and Edit, Save, social linking and every protected action remain unavailable until the settled provider confirms the address. A disconnected or different-wallet outcome still replaces the preview through the existing wallet-state event. This preserves the A-46 deferred SDK boundary, the A-47 sole settlement writer and A-03/A-05/A-45 acceptance. Status stays `in progress` until production iOS and Android reload/navigation acceptance. |
| A-54 | Reduce the 3-5 second perceived load on every page | Founder desktop report 2026-08-19, new account, production; cold Chromium trace 2026-08-21 | done | A | Cold production traces confirm that paint is already fast (roughly 0.16-0.92s), while `DOMContentLoaded` waits on the wallet dependency graph. The home route loaded 235 scripts, including 218 `esm.sh` resources; replacing only `appkit-init.js` with an inert diagnostic module reduced the same cold run to 57 scripts, 39 `esm.sh` resources and roughly 0.77s `DOMContentLoaded`, proving that the AppKit/Wagmi CDN graph is the dominant shared-page delay. The safe repair does not reintroduce the rejected lazy wallet shim and does not change any wallet API: the exact existing SDK versions are now pinned as project dependencies and the existing `appkit-init.js`, `wallet-core-connect.js` and diagnostic entry are processed by Vite into same-origin chunks. The external mobile Ethereum provider remains demand-loaded only when Connect needs it. A matched cold Android-browser trace on Vercel preview #206 reduced the home route from 250 to 125 resources, 241 to 96 scripts, 216 to 46 `esm.sh` requests and roughly 1.94s to 0.88s `DOMContentLoaded`, while both production and preview first-painted at roughly 0.36s. The preview also opened the production `wcm-modal` mobile WalletConnect surface and did not mount the experimental AppKit `w3m-modal`. Status remains `in progress` until real iOS/Android wallet acceptance confirms the improvement without a connection regression. Closed 2026-08-21 by measurement on the apex, mobile viewport, no throttling: TTFB 141 ms, first contentful paint 444 ms, DOMContentLoaded 942 ms, load 944 ms. The reported 3-5 seconds is gone. The Tailwind CDN removal (A-46) and the card sizing (A-60) between them account for it. **Verified by measurement in a browser, not by founder acceptance on a device.** |
| A-55 | Stop product stylesheets from overriding Tailwind layout utilities | Forensic audit 2026-08-20; browser verification on production 2026-08-21 at 375px; `performance-optimizations.css`; `mobile-responsive.css`; `test/grid-utility-collision.test.cjs` | done | A | `.grid` and `.flex` are Tailwind display utilities. Declaring layout on them reaches every element on the page, and because both selectors weigh one class and neither used `!important`, source order decided - the build emits these files after `tailwind-build.css`, so the utility lost. The first pass cleaned `performance-optimizations.css` only, and the row was reported as fixed. **It was not.** `mobile-responsive.css` carried the same overrides inside `@media` blocks and Vite bundles it into the same emitted chunk, so production kept the defect. Verified in a real browser on the apex at 375px: the profile gallery resolved to one 318.4px column with a 16px gap instead of the two 153.2px columns and 12px gap its own `grid-cols-2 gap-3` asks for. A third rule, `.flex { gap: 8px }` under 380px, changed nothing measurable on the pages checked - the only unqualified `gap-*` in use was `gap-2`, which already wants 8px - and is removed as the same latent trap, not for a symptom. `header > .container > .flex` is deliberately left: a scoped selector styling one known element is not utility shadowing. The guard test missed all of this because its rule extractor was a single regex over the whole file and could only ever see top-level rules; it is now a depth-aware scan that reaches inside `@media`, and it compares selector tokens rather than a constructed regex, whose escapes had collapsed so that `.profile-artwork-grid` matched too. Status stays `in progress` until production acceptance confirms two columns on mobile. Closed 2026-08-21 by measurement on the merged build at 375px: the profile gallery resolves to two 153.2px columns with a 12px gap, and to six columns at desktop width. Both match the element's own `grid-cols-*`. **Verified by measurement in a browser, not by founder acceptance on a device.** |
| A-56 | Stop the header naming an account by its connection status | Founder Android and iOS production evidence 2026-08-21; `header-prepaint.js`; `unified-styles.css` `wallet-state-resolving`; `test/header-identity-no-status-word.test.cjs` | in progress | A | On every page navigation the header read `Connecting...` beside an address it already knew, and the founder saw it twice in sequence. There are two surfaces: the static `[data-avatar-resolving]` overlay shown while `html.wallet-state-resolving` is set, and the label written into `[data-avatar-name]`, which that class hides and the component later reveals. The prepaint gated its identity branch on a validated base64 avatar preview. An account with no profile picture uses the neutral avatar and therefore never has a preview, so it fell through to the status label on every navigation, permanently. Fixed at the label only: when the cached avatar **is** the neutral image the account has nothing left to resolve, so its cached name - which for a nameless account is its own shortened address - is painted instead of a status word. An account whose custom avatar has not decoded yet keeps the resolving label, because A-46 established that a real name beside the neutral placeholder is a half identity. The image half of that branch is untouched, so the button still never blanks. Status stays `in progress` until Android and iOS production runs confirm no status word appears on any page while connected. |
| A-57 | Distinguish wallet capability limits from session failures in the network switch classifier | Founder production report 2026-08-19, Robinhood Wallet on iOS; forensic audit 2026-08-19; `appkit-init.js` `describeNetworkSwitchFailure`, `switchArtSoulNetwork`, `confirmCoreBaseSepolia` | planned | A | Selecting Base Sepolia produced `Could not switch to Base Sepolia. Reconnect the wallet and try again.`, which is the classifier's catch-all. Reconnecting cannot help when the wallet does not offer the chain, and the same string is emitted for at least four unrelated causes. `isUnknownChainError` exists but the classifier never consults it, and the `BASE_SEPOLIA_REQUIRED` thrown by a confirmation timeout also falls through, so an accepted-but-unconfirmed switch is reported as a dead session. EIP-1193 `4200` and WalletConnect `5100`-`5102` are recognised nowhere. Robinhood first-party documentation lists Base among nine supported networks and does not mention testnets or custom EVM networks; that is absence of evidence, not a denial, so this is **not** established as an ArtSoul defect. The founder accepts that wallets without custom-testnet support cannot use the public testnet and treats that as a reach limit rather than a bug. The selector path can also issue `wallet_addEthereumChain` twice and `wallet_switchEthereumChain` up to four times per attempt. Capture masked provider errors per stage on `/wallet-test` before implementing, keep messages vendor-neutral, add no chain, and do not weaken `ensureArtSoulWriteNetwork`. |
| A-59 | Detect an account switch made inside the wallet application | Founder report 2026-08-21 using a wallet holding four accounts; `appkit-init.js` `handleProviderAccountsChanged` | planned | A | Switching to a different account inside the wallet is not noticed: the site keeps browsing as the previously approved address, and no reconnect, re-approval or fresh signature is requested. Each account is a separate identity, so this is an identity-integrity concern rather than cosmetics. The handler itself is correct - it compares against `lastProcessedAddress` and calls `signOutMismatchedSession` - so the likely cause is that the wallet never emits `accountsChanged` or a WalletConnect `session_update`, and a session's approved account list is fixed at approval time. That cannot be inferred from the dapp side without evidence. Capture on a real device: the raw `session.namespaces.eip155.accounts` before and after the in-wallet switch, whether any `accountsChanged` or `session_update` arrives, and the provider address on visibility return. Only then decide between a reconciliation read and a documented wallet limitation. Do not add polling of the provider account on a timer and do not reopen A-03, A-05 or A-45 acceptance. Reported on wallets other than MetaMask. |
| A-58 | Keep the previous Profile Sections result on screen until the next one is ready | Founder iOS production evidence 2026-08-20; forensic audit; `src/entries/profile.jsx`; `unified-styles.css`; `test/profile-tab-content-continuity.test.cjs` | done | A | Switching between Created Artworks, Auctions, Sales and Owned NFTs destroyed the current result in the same frame as the click, because `setArtworksLoading(true)` was applied synchronously in the handler and the render was a ternary. The replacement was a fixed six-card skeleton whose media is `aspect-ratio: 1`, so the container took a height unrelated to the incoming tab and then collapsed. Under the A-55 defect that height was 3122 px at mobile width. Fixed by committing the list and the tab it belongs to in one guarded step: `displayedGallery` lags `selectedGallery` until a fetch resolves, and the heading, item count, empty message and Add New tile all follow the committed value, so the heading can never describe a list it is not showing. The tapped tab still highlights immediately, because that feedback must not lag. The skeleton is kept for a first load, where there is no previous result to preserve. The stale state is signalled by opacity on `aria-busy` plus a compact inline note, with no theme colour, no `min-height`, no timeout and no overflow masking. A per-tab cache was rejected: it still flashes on the first switch and risks diverging from indexer projection truth. The existing `artworksRequestRef` guard was correct and is unchanged. Status stays `in progress` until production acceptance on iOS and Android confirms no oversized strip and no collapse when switching into an empty tab. Closed 2026-08-21 by browser verification: 40 rapid tab taps produced no frame where the heading described a list it was not showing, and the outgoing list is never dimmed. Repeated under Slow 3G with 6x CPU throttling with the same result and no height jump. **Verified by measurement in a browser, not by founder acceptance on a device.** |
| A-60 | Request stored images at a display size instead of the original upload | Founder production report 2026-08-21 ("works render black for two to three seconds", "Connecting flashes on every page"); live storage measurement 2026-08-21; `supabase-client.js` `storageRenderUrl`; `src/ui/components/artwork-card.js`; `avatar-dropdown.js`; `src/entries/profile.jsx`; `src/entries/artwork.jsx`; `test/card-thumbnail-transform.test.cjs` | done | A | Every card and every avatar requested the full original upload and scaled it down in the browser. Measured against the live bucket with a browser `Accept` header so format negotiation applies: a 1668 KB artwork PNG returns as 68 KB WebP at `width=600&quality=70`; profile avatars of 2045, 2313 and 2841 KB return as 8.3, 10.1 and 28.4 KB at a display width. Without that header the endpoint returns JPEG or PNG and a small file grows instead, which is why an earlier measurement of this defect was wrong and is not repeated here. The avatar half is the more serious of the two: `decodeThenCommitIdentity` holds the identity commit until the avatar decodes, so a multi-megabyte avatar is what kept the resolving label on screen after A-56 fixed the nameless-account case. It also explains why the base64 preview was rarely available - the capture only happens after that decode. A-55 made the card half visible rather than causing it, since restoring the second mobile column doubled the bytes on the first screen. One helper, `ArtSoulSecurity.storageRenderUrl`, lives beside `isValidStorageUrl` in `supabase-client.js` and is the only place the render URL is built; cards, the header button and menu, the profile hero and the artwork provenance rows all call it with their own display width. Stored, projected and linked URLs are untouched, so detail views and fullscreen keep full quality. Only a raster still the URL itself vouches for is resampled - `jpg`, `jpeg`, `png`, `webp`, `avif` - on our own storage host and passing `isValidStorageUrl`. GIF is excluded because a still transform drops its animation, SVG because rasterising it loses the point, and an extensionless upload is excluded because production rows carry a bare `image` in `file_type` rather than a MIME type, so the extension is the only honest signal. The neutral avatar passes through unchanged, which A-56's nameless-account branch depends on and a test now pins. The render endpoint returns `access-control-allow-origin: *`, so the preview capture and the prepaint preload both still work. Status stays `in progress` until production acceptance confirms cards paint without a dark interval and no status word appears in the header, on iOS and Android. Card half closed 2026-08-21 by browser verification at 375px: 19 of 21 storage images are served from the render endpoint; the two exceptions are a `.gif`, correctly excluded so its animation survives, and the avatar, which A-61 returns to its original on purpose. The avatar half of this row is superseded by A-61. **Verified by measurement in a browser, not by founder acceptance on a device.** |
| A-61 | Display every avatar at its own proportions, and make the cached preview reliable | Founder report 2026-08-21 (avatars cropped or enlarged; "Connecting" still appears when opening, closing, swiping and leaving artwork, docs or publish pages on iOS); live avatar dimension measurement 2026-08-21; `avatar-dropdown.js`; `src/entries/profile.jsx`; `src/entries/artwork.jsx` | in progress | A | A-60 requested avatars through the render endpoint at a fixed width. That forced every upload through one dimension: measured on the live bucket, avatars are 400x400, 832x1248, 1254x1254 and 4032x3024, so the non-square ones were reframed and the small ones upscaled. Reverted - `getProfileAvatarUrl` returns the upload itself on all three surfaces, and a test pins that none of them resizes what it displays. Artwork cards keep their 600px thumbnail: a card is a crop of a work, not a portrait, and the dark-card report is separate. The header symptom has a different cause and is fixed at that cause. `decodeThenCommitIdentity` captured the base64 preview from the image it had already decoded, which cost no extra request but could only run after a multi-megabyte upload finished decoding; anyone who moved on before then left with no preview stored, so the next document had nothing to paint and resolved from scratch again - on every navigation, permanently. The capture now issues its own request for a 128px copy that is never displayed, decodes in one frame, and stores only its pixels; when no smaller copy exists - off-host media, a GIF, an SVG - it falls back to the decoded display image, which is the previous behaviour exactly. The superseded "no second request" assertion in `shared-header-navigation-flicker` is replaced by one describing what is actually wanted, with the reason recorded. Status stays `in progress` until an iOS run confirms avatars are unreframed and no status word appears while navigating. |
| A-62 | Keep the way back to your own profile while reading someone else's | Founder production report 2026-08-21 with both URLs; `avatar-dropdown.js` `isCurrentNavigationItem`, `renderDropdownNavItems`, new `isOwnProfileAddress`; `test/profile-link-on-other-profiles.test.cjs` | in progress | A | Opening another person's profile removed the Profile entry from the account menu, so there was no way back to your own except editing the URL. `renderDropdownNavItems` filtered an entry out when its `path` matched the current one, and `/profile` is the path for everybody's profile: `/profile?address=<someone else>` counted as "you are already here". The answer was already being computed - every call site derived `isOwnProfile` and passed it in - and the renderer simply ignored it. `isCurrentNavigationItem` now takes it and treats the Profile entry as current only on your own profile. The comparison itself was written out four separate times with slightly different sources for the wallet; it is now one method, `isOwnProfileAddress`, which answers the same question everywhere: `/profile` with no address is always yours, with one only when it matches, case-insensitively. The restoring frame previously assumed any profile page was yours, which hid the link during session restore as well; it now asks the same method. The two URL shapes the founder observed are both correct and stay: the account menu links to `/profile`, and an artwork links to `/profile?address=<creator>`; when that address is your own the page is your profile, Edit included. Status stays `in progress` until a connected run confirms the entry appears on another person's profile and stays hidden on your own. |
| A-63 | Start a page's own data request before its modules exist | Founder report 2026-08-21 (profile lags on its default tab and on switching, Android and iOS); measurement on the apex 2026-08-21 with an iPhone viewport and a 6x CPU slowdown; `data-prefetch.js`; `supabase-client.js` `backendRead`; every shared-header page | in progress | A | Measured, not inferred: the document is interactive at 674 ms, the modules finish executing at 3276 ms, and the request for the page's own content is not issued until 3410 ms - because it waits for them. 136 script files, 504 KB, of which the wallet SDK is the bulk (`SIWXUtil` alone is 127 KB). The screen holds nothing for 2.7 seconds, the skeleton appears at 3373 ms, and the first real content at 5833 ms. The network is idle for all of it. `data-prefetch.js` is a small classic script loaded `async` from every shared-header page: it builds the exact request the page will make - `/api/public/config` everywhere, `/api/public/artworks?creator=<wallet>&limit=200` on a profile, `?limit=100` on the homepage - and issues it immediately, so the fetch overlaps the module work instead of queueing behind it. `backendRead` consumes it when the path matches. It is a head start and never a cache: each response is handed to exactly one consumer and then dropped, so a second read, a different path, or the script's absence all fall through to the normal request. Address casing is folded on the key alone, because the URL carries the checksummed form and the database row is lower case. It is `async` rather than blocking: only the prepaint bridge may hold up parsing, and this is not a first-paint concern. The 504 KB itself is untouched and is the larger remaining cost; deferring the wallet SDK off the critical path is a separate change and is not attempted here. Status stays `in progress` until an Android and iOS run confirms the profile paints sooner. |

## Phase B - Public Beta

| ID | Item | Why/source | Status | Phase | Notes |
| --- | --- | --- | --- | --- | --- |
| B-01 | Recruit and support a controlled tester cohort | Canon [B1](canon/12_IMPLEMENTATION_BACKLOG.md); [`17_ROADMAP_PHASES.md`](canon/17_ROADMAP_PHASES.md) | planned | B | Track artists and collectors through GitHub Issues rather than private chat-only notes. |
| B-02 | Verify complete publish-to-resale journeys with real users | Canon [B1-B3](canon/12_IMPLEMENTATION_BACKLOG.md); [`HANDOFF.md`](HANDOFF.md) | planned | B | Publish -> auction -> settlement -> mint -> resale, including moderation and wallet recovery cases. |
| B-03 | Operate monitoring and an incident runbook | Canon [B2](canon/12_IMPLEMENTATION_BACKLOG.md); [`PROJECT_STATE.md` P2](PROJECT_STATE.md) | planned | B | Cover health, errors, indexer lag, cost, moderation and cached public metrics with named response steps. |
| B-04 | Process beta defects as evidence-backed focused work | Canon [B3](canon/12_IMPLEMENTATION_BACKLOG.md) | planned | B | Do not mix protocol redesign into UI/UX defect fixes. |
| B-05 | Build the versioned Snapshot A exporter and verifier | Canon [B4](canon/12_IMPLEMENTATION_BACKLOG.md); Bible [section 16](canon/ARTSOUL_CANON_BIBLE_FULL.md) | planned | B | Repository search found no participant Snapshot A exporter. Output must be machine-readable, reproducible, hashed and independently verifiable after the live DB reset. The preferred cutover uses a fresh mainnet database, so Snapshot A and its two durable copies must remain readable without the testnet database. |
| B-06 | Announce, capture and durably replicate Snapshot A | Canon [B4](canon/12_IMPLEMENTATION_BACKLOG.md); [`CHANGELOG_2026-07-16_ROADMAP_ALIGNMENT.md`](canon/CHANGELOG_2026-07-16_ROADMAP_ALIGNMENT.md) | planned | B | Announce the cut-off at least two weeks ahead and keep two independent durable copies. It is a community record and creates no entitlement. |
| B-07 | Run the Base build-in-public and cohort loop | Canon [B5](canon/12_IMPLEMENTATION_BACKLOG.md); [`17_ROADMAP_PHASES.md`](canon/17_ROADMAP_PHASES.md) | planned | B | Use themed drops, personal onboarding and verified cached metrics; partnerships/campaigns follow evidence. |

## Phase C - Mainnet Preparation

| ID | Item | Why/source | Status | Phase | Notes |
| --- | --- | --- | --- | --- | --- |
| C-01 | Provide ProtocolTreasury and EcosystemTreasury Safe addresses and rehearsal Safes | Canon [C0](canon/12_IMPLEMENTATION_BACKLOG.md); [`CONTRACT_REWORK_PLAN.md` Part 3](canon/CONTRACT_REWORK_PLAN.md); [`RESOURCE_GATED_WORK.md`](RESOURCE_GATED_WORK.md); [`RG05_SAFE_MULTISIG_REHEARSAL_2026-08-16.md`](testnet/RG05_SAFE_MULTISIG_REHEARSAL_2026-08-16.md) | blocked-on-founder | C | Addresses and signer policy block role handover, pool routing, Collections and official/platform status binding. The rehearsal half advanced on 2026-08-16 and was verified on chain on 2026-08-19: a 2-of-3 Base Sepolia Safe v1.4.1 with three independently held signers enforced its threshold, all three signer pairs executed, the pair excluding the founder executed on its own, and a `swapOwner` replacement removed an owner without that owner's signature before the original composition was restored. Grant and revoke runbooks are still not exercised, so RG-05 stays open. That rehearsal Safe holds testnet ETH only and is not a mainnet Safe candidate. Mainnet ProtocolTreasury and EcosystemTreasury addresses plus their signer policy remain undecided, so this row stays blocked-on-founder. |
| C-02 | Acquire the project domain and project email | Canon [C0/C14](canon/12_IMPLEMENTATION_BACKLOG.md); [`CHANGELOG_2026-07-13_AMENDMENT.md`](canon/CHANGELOG_2026-07-13_AMENDMENT.md); [`RESOURCE_GATED_WORK.md`](RESOURCE_GATED_WORK.md) | blocked-on-founder | C | `artsoulprotocol.com` is acquired and live with the apex, permanent `www` redirect, canonical metadata and bounded Vercel rollback verified on 2026-08-08. The combined item remains blocked only on a monitored project mailbox and its general/security/copyright operating procedure. |
| C-03 | Approve and record an operational Genesis grant cadence | [`17_ROADMAP_PHASES.md`](canon/17_ROADMAP_PHASES.md); [`CHANGELOG_2026-07-16_ROADMAP_ALIGNMENT.md`](canon/CHANGELOG_2026-07-16_ROADMAP_ALIGNMENT.md) | blocked-on-founder | C | Cadence is tunable policy, not canon and not an immutable contract parameter. |
| C-04 | Decide the Ecosystem Pool contest trigger | Canon [C6](canon/12_IMPLEMENTATION_BACKLOG.md); [`CHANGELOG_2026-07-16_ROADMAP_ALIGNMENT.md`](canon/CHANGELOG_2026-07-16_ROADMAP_ALIGNMENT.md) | blocked-on-founder | C | No date, count or percentage is implied until founder/treasury design approval. Frozen 1% routing does not change. |
| C-05 | Decide legal entity form/jurisdiction with counsel | Canon [C14](canon/12_IMPLEMENTATION_BACKLOG.md); [`17_ROADMAP_PHASES.md`](canon/17_ROADMAP_PHASES.md) | blocked-on-founder | C | Legal form remains a founder/counsel decision, not canon. |
| C-06 | Complete contract research, threat model and storage-layout sign-off | Canon [C1](canon/12_IMPLEMENTATION_BACKLOG.md); [`CONTRACT_REWORK_PLAN.md`](canon/CONTRACT_REWORK_PLAN.md) | planned | C | Resolve upgrade, keeper, Collections and role-design questions before Solidity changes. |
| C-07 | Rework Core with frozen economics and bounded finalization/refunds | Canon [C2](canon/12_IMPLEMENTATION_BACKLOG.md); Bible [sections 2-3](canon/ARTSOUL_CANON_BIBLE_FULL.md); [`CONTRACT_REWORK_PLAN.md`](canon/CONTRACT_REWORK_PLAN.md) | planned | C | Use deterministic obligations, bounded batches and withdrawable-credit fallback. No unbounded bidder loop and no unmotivated losing bidder required to progress. |
| C-08 | Replace ArtworkNFT with canonical royalty and marketplace enforcement | Canon [C3](canon/12_IMPLEMENTATION_BACKLOG.md); [`CONTRACT_REWORK_PLAN.md`](canon/CONTRACT_REWORK_PLAN.md) | planned | C | Correct the deployed prototype's 7.5% royalty to canonical 5.5% and enforce marketplace approvals without changing frozen resale splits. |
| C-09 | Build the dedicated soulbound ArtSoulGenesis contract | Canon [C4](canon/12_IMPLEMENTATION_BACKLOG.md); Bible [section 7](canon/ARTSOUL_CANON_BIBLE_FULL.md) | planned | C | Mainnet-only design with public eligibility categories, durable grant records, audit log and multisig-authorized controls. |
| C-10 | Build canonical Partner Collections | Canon [C5](canon/12_IMPLEMENTATION_BACKLOG.md); Bible [section 8](canon/ARTSOUL_CANON_BIBLE_FULL.md) | planned | C | First auction, max attempts/cooldown and flat buy-now behavior reuse canonical per-artwork floor economics. |
| C-11 | Implement Ecosystem Pool accounting and treasury routing | Canon [C6](canon/12_IMPLEMENTATION_BACKLOG.md); Bible [section 14](canon/ARTSOUL_CANON_BIBLE_FULL.md) | planned | C | Route the frozen 1% resale allocation; contest activation remains C-04. |
| C-12 | Rework indexer/database projections for the final contracts | Canon [C7](canon/12_IMPLEMENTATION_BACKLOG.md); [`CONTRACT_REWORK_PLAN.md`](canon/CONTRACT_REWORK_PLAN.md) | planned | C | Include lifecycle, grants, pool, provenance, moderation and migration evidence. |
| C-13 | Build least-privilege admin and eligibility controls | Canon [C8](canon/12_IMPLEMENTATION_BACKLOG.md); [`CHANGELOG_2026-07-16_ROADMAP_ALIGNMENT.md`](canon/CHANGELOG_2026-07-16_ROADMAP_ALIGNMENT.md) | planned | C | Public categories plus durable grant records, audit logs and multisig gates; hidden anti-abuse details stay private. |
| C-14 | Bind canonical aura priority to real indexed/on-chain status | Canon [C9](canon/12_IMPLEMENTATION_BACKLOG.md); [`16_VISUAL_STATUS_AND_THEME_CANON.md`](canon/16_VISUAL_STATUS_AND_THEME_CANON.md) | planned | C | Bind Platform > Genesis > Partner > none to the A-34 frame on cards and artwork pages. Users never choose colors. |
| C-15 | Implement Top-100k as a profile badge | Canon [C9](canon/12_IMPLEMENTATION_BACKLOG.md); Bible [section 7.6](canon/ARTSOUL_CANON_BIBLE_FULL.md) | planned | C | It is a separate soulbound numbered profile badge, not an artwork aura or tradeable NFT. |
| C-16 | Consolidate and implement promoted-banner rules | Canon [C10](canon/12_IMPLEMENTATION_BACKLOG.md); [`CHANGELOG_2026-07-16_ROADMAP_ALIGNMENT.md`](canon/CHANGELOG_2026-07-16_ROADMAP_ALIGNMENT.md) | planned | C | Consolidate the planning delta into the Bible and decide whether any reserve is fixed or tunable before implementation. Organic ranking must remain unaffected. |
| C-17 | Route project works and official/platform frame from the canonical project wallet | `src/features/discovery/discovery-service.js` TODO; `unified-styles.css` TODO; [`PROJECT_STATE.md` P2](PROJECT_STATE.md) | blocked-on-founder | C | Both code TODOs depend on C-01. Do not guess an address. |
| C-18 | Decide whether contract-level NFT offers belong in scope | `src/entries/gallery.jsx` TODO | blocked-on-founder | C | Current contracts do not support offers and canon does not define them. Keep the UI absent unless a dated product/canon decision approves mechanics. |
| C-19 | Remove orphan compiled contract artifacts | [`CONTRACT_REWORK_PLAN.md` Part 1](canon/CONTRACT_REWORK_PLAN.md) | planned | C | Remove stale Marketplace/Token artifacts during the rework; never revive token code. |
| C-20 | Add complete contract, invariant, role and migration suites | Canon [C11](canon/12_IMPLEMENTATION_BACKLOG.md); [`CONTRACT_REWORK_PLAN.md`](canon/CONTRACT_REWORK_PLAN.md) | planned | C | Cover refunds, failed recipients, finalization, idempotency, reentrancy, economics and storage layout. |
| C-21 | Procure and pass independent security review | Canon [C12](canon/12_IMPLEMENTATION_BACKLOG.md); [`17_ROADMAP_PHASES.md`](canon/17_ROADMAP_PHASES.md); [`RESOURCE_GATED_WORK.md`](RESOURCE_GATED_WORK.md) | blocked-on-founder | C | Resource-gated but mandatory. Founder procures the audit only after architecture and internal invariants are ready; engineering resolves every launch-blocking finding before approval. Lack of funding cannot waive this gate or promote prototype contracts to mainnet. |
| C-22 | Run a fresh Base Sepolia topology rehearsal | Canon [C13](canon/12_IMPLEMENTATION_BACKLOG.md) | planned | C | Deploy final contracts, transfer rehearsal roles to Safes and exercise full lifecycle, pool and recovery runbooks. |
| C-23 | Complete legal, privacy, IP and operational readiness | Canon [C14](canon/12_IMPLEMENTATION_BACKLOG.md) | blocked-on-founder | C | Counsel/founder-owned inputs; engineering may implement approved technical requirements only after decisions exist. |
| C-24 | Perform the final premium visual/accessibility pass | Canon [C15](canon/12_IMPLEMENTATION_BACKLOG.md) | planned | C | Homepage, cards, artwork, aura, responsive and accessibility polish occurs after functionality is stable. |
| C-25 | Approve a funded mainnet launch and operating envelope | Canon [C14/D3](canon/12_IMPLEMENTATION_BACKLOG.md); [`RESOURCE_GATED_WORK.md`](RESOURCE_GATED_WORK.md); [`BASE_ECOSYSTEM_FUND_READINESS.md`](BASE_ECOSYSTEM_FUND_READINESS.md) | blocked-on-founder | C | Before launch approval, current quotes and committed funds must cover independent review, domain/email, Safes/signers, rehearsal and mainnet gas, monitoring, backups, incident response, legal readiness, renewals, and a defined operating runway. The Base Ecosystem Fund path is currently researched only: prepare after Phase A, target submission after initial verified Phase B external-user evidence, and require separate founder approval. Applications, diligence, offers and expected grants are not committed funds. |
| C-26 | Build `Support the artist` as an event-only creator-donation path | Founder decision recorded in [`SUPPORT_THE_ARTIST_DESIGN.md`](canon/SUPPORT_THE_ARTIST_DESIGN.md); Canon Bible [sections 6, 11, 14 and 17](canon/ARTSOUL_CANON_BIBLE_FULL.md); Canon [C1, C7, C8, C11-C13](canon/12_IMPLEMENTATION_BACKLOG.md) | planned | C | Documentation only during Phase A. In Phase C, add a minimal audited contract that verifies the artwork's canonical creator, forwards 100% immediately with no custody or protocol fee, emits the donation/message/anonymity event, and stores no history. Index it through the bounded event filter and cached projection path. A non-empty message requires the configurable `0.0005 ETH` initial threshold, reconfirmed at deployment with no oracle; messages are plain text, limited to 140 user-visible characters, never linkified, and may be hidden on-site through complaint-driven moderation while remaining public on-chain. Donations never affect Trust, discovery, floor, price, ownership, settlement, royalties, treasury accounting, or any protocol outcome. No Phase B version. Before implementation, create the dated canon consolidation for Trust/discovery neutrality, message moderation, treasury bypass, and the additional contract in the Phase C topology. |

## Phase D - Staged Mainnet Launch

| ID | Item | Why/source | Status | Phase | Notes |
| --- | --- | --- | --- | --- | --- |
| D-01 | Re-verify Snapshot A and its independent copies | Canon [D1](canon/12_IMPLEMENTATION_BACKLOG.md); Bible [section 16](canon/ARTSOUL_CANON_BIBLE_FULL.md) | planned | D | Verify manifest, schema and post-reset readability before destructive migration. |
| D-02 | Execute the ordered pre-mainnet reset and migration | Canon [D2](canon/12_IMPLEMENTATION_BACKLOG.md); Bible [section 16](canon/ARTSOUL_CANON_BIBLE_FULL.md) | planned | D | Prefer a fresh mainnet database/project; preserve the testnet environment only as a controlled archive. Do not import testnet artworks, auctions, bids, signals, floors, ownership, settlement, trust, or derived profile statistics as live mainnet state. Apply the final schema with a complete ledger, configure new contracts/start blocks, and prove zero stale testnet connections before opening writes. |
| D-03 | Deploy audited Core operations to Base | Canon [D3](canon/12_IMPLEMENTATION_BACKLOG.md); [`17_ROADMAP_PHASES.md`](canon/17_ROADMAP_PHASES.md); [`RESOURCE_GATED_WORK.md`](RESOURCE_GATED_WORK.md) | planned | D | Resource-gated by C-25. Deploy only audited artifacts, hand roles to Safes, and open writes only after production smoke tests pass. Never deploy the current prototype contracts to reduce launch cost. |
| D-04 | Observe Core through an approved stability window | Canon [D4](canon/12_IMPLEMENTATION_BACKLOG.md); [`CHANGELOG_2026-07-16_ROADMAP_ALIGNMENT.md`](canon/CHANGELOG_2026-07-16_ROADMAP_ALIGNMENT.md) | planned | D | Duration is a launch-readiness decision, not frozen canon. |
| D-05 | Run Genesis as a separate bounded pilot | Canon [D5](canon/12_IMPLEMENTATION_BACKLOG.md); [`17_ROADMAP_PHASES.md`](canon/17_ROADMAP_PHASES.md) | planned | D | Activate only after Core stability and expand only through the audited grant path. |
| D-06 | Scale controlled growth from verified evidence | Canon [D6](canon/12_IMPLEMENTATION_BACKLOG.md) | planned | D | Expand onboarding, Collections, contests, partner work and marketing only after monitored production evidence. |

## Intake Rule For Future Work

When a new idea arrives, add one row with a stable ID, an in-repository or PR source, an evidence-based status, a canonical phase, and any founder dependency. If it conflicts with architecture, record the conflict and create a separate dated canon-amendment decision before implementation. UI ideas may evolve, but they still enter this backlog first so completed work is not unknowingly rebuilt.
